-- ============================================================
-- PM Operating System — 0028 Unify all tasks on gemini-3.5-flash
--
-- تصحيح لتقييم 0027: gemini-3.5-flash موديل رسمي حقيقي (GA، صدر مايو
-- 2026)، مش اسم خاطئ. القرار: كل أنواع المهام تستخدم نفس الموديل الأحدث
-- بدل تشتيتها بين 2.5 و3.5. تحديث بيانات فقط (UPDATE)، بلا تغيير Schema.
-- ============================================================

update public.ai_task_model_config
set model = 'gemini-3.5-flash'
where provider = 'gemini';

-- ============================================================
-- انتهى 0028
-- ============================================================
