# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# النسخ الاحتياطية — بيانات عملاء حقيقية، ممنوع دخولها المستودع نهائيًا.
backups/

# ملف متغيّرات Railway — يحتوي أسرارًا، يُنشأ محليًا ولا يُرفع أبدًا
railway-variables.txt

# scratch: local working files (contain secrets, test data)
scratch/
